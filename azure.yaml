# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: DaprAlbums
services:
  # uncomment once we want to deploy everything using AZD w/o VS
  # albumapi:
  #   project: albumapi
  #   dist: build
  #   language: csharp
  #   host: containerapp
  #   module: app/albumapi
  albumviewer:
    project: albumviewer
    language: js
    host: containerapp
    module: app/albumviewer
